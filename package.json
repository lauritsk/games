{
  "name": "games",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server/index.ts",
    "build": "bun build ./index.html --outdir=dist --minify && bun build ./src/ui/service-worker.js --outdir=dist --minify --entry-naming='[name].[ext]' && cp src/ui/favicon.svg dist/favicon.svg",
    "build:single": "bun build --compile --target=browser ./index.html --outdir=dist-single --minify",
    "typecheck": "tsc --noEmit",
    "test": "bun test test/*.test.ts",
    "test:e2e": "playwright install chromium && playwright test",
    "test:watch": "bun --watch test/*.test.ts",
    "check": "bun run typecheck && bun run test && bun run build && bun run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/bun": "latest",
    "typescript": "^6"
  }
}
