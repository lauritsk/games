amends "package://github.com/jdx/hk/releases/download/v1.44.3/hk@1.44.3#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v1.44.3/hk@1.44.3#/Builtins.pkl"

local linters = new Mapping<String, Step> {
  ["oxlint"] = new Step {
    glob = List("**/*.{js,jsx,ts,tsx}")
    check = "vp lint {{ files }}"
    fix = "vp lint --fix {{ files }}"
  }
  ["oxfmt"] = new Step {
    glob = List("**/*.{js,jsx,ts,tsx,json,jsonc,css,html}")
    check = "vp fmt --check {{ files }}"
    fix = "vp fmt {{ files }}"
  }
  ["typecheck"] = new Step {
    glob = List("**/*.{ts,tsx}")
    check = "vp run typecheck"
  }
  ["mise"] = Builtins.mise
}

hooks {
  ["pre-commit"] {
    fix = true
    stash = "git"
    steps = linters
  }
  ["fix"] {
    fix = true
    steps = linters
  }
  ["check"] {
    steps = linters
  }
  ["commit-msg"] {
    steps {
      ["cocogitto-commit-msg"] = Builtins.cocogitto_commit_msg
    }
  }
}
